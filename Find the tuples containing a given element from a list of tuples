##Find the tuples containing a given element from a list of tuples.

input_tuple_list = [(11, 22), (33, 55), (55, 77), 
         (11, 44), (33, 22, 100, 11), (99, 11)] 

## n will store the value of the element to be searched in the list of tuples. 
n=11 

#filter 
output_tuple = list(filter(lambda x:11 in x, input_tuple_list)) 
  
# Prinitng output 
print(output_tuple) 
